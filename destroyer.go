package main

/*
func destroyer(err error, label, projectName string) {
	if os.IsExist(err) {
		os.Exit(98)
	}

	err = os.Chdir("..")
	if err != nil {
		fmt.Println(err)
		os.Exit(99)
	}

	err = os.RemoveAll(projectName)
	if err != nil {
		fmt.Println(err)
		os.Exit(100)
	}

	fmt.Println(label)
}
*/
